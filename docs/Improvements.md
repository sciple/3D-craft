# Imrpovements and new features
[x] A new function that prepares elements for print: all modeled parts are arranged on a grid and placed on the floor (i.e. their bottoms are aligned on the z axis).
[] Alert user when closing the application . right now closing the window directly quits the app and everything in the modeling area is lost